@inject UserManager<MotorGliding.Models.Db.User> userManager 
<div class="row">
        <div class="col-12">
            <nav class="navbar navbar-expand-xl navbar-light bg-light">
                <a class="navbar-brand" asp-action="Index" asp-controller="Home">
                    @*<i class="fas fa-3x fa-tachometer-alt tm-site-icon"></i>
                    <h1 class="tm-site-title mb-0">Buzz-tandem</h1>*@
                    <img src="~/img/logo2.png" />
                </a>
                <button class="navbar-toggler ml-auto mr-0" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item @(Html.Raw(ViewBag.Active == MotorGliding.Models.Enums.Tabs.Dashboard ? "active" : "")) />">
                            <a class="nav-link" asp-action="Index" asp-controller="Dashboard">Dashboard</a>
                        </li>
                        @*<li class="nav-item">
            <a class="nav-link" asp-action="Calendar" asp-controller="Dashboard">Kalendarz</a>
        </li>*@

                        @*<li class="nav-item @(Html.Raw(ViewBag.Active == "Account" ? "active" : "")) />">
            <a class="nav-link" asp-action="Account" asp-controller="Dashboard">Konto</a>
        </li>*@
                        <li class="nav-item dropdown @(Html.Raw(ViewBag.Active == MotorGliding.Models.Enums.Tabs.Settings ? "active" : ""))">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
                               aria-expanded="false">
                                Konto
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" asp-action="Edit" asp-controller="Account" asp-route-id="">Edytuj dane</a>
                                <a class="dropdown-item" asp-action="EditPass" asp-controller="Account">Zmień hasło</a>
                            </div>
                        </li>
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item @(Html.Raw(ViewBag.Active == MotorGliding.Models.Enums.Tabs.Gallery ? "active" : "")) />">
                                <a class="nav-link" asp-action="List" asp-controller="Gallery">Galeria</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-action="List" asp-controller="Event">Oferty</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
                                   aria-expanded="false">
                                    Ustawienia
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    @*<a class="dropdown-item" href="#">Profil</a>*@
                                    <a class="dropdown-item" asp-action="UserList" asp-controller="Account">Użytkownicy</a>
                                    <a class="dropdown-item" asp-action="List" asp-controller="Vehicle">Sprzęt</a>
                                    @*<a class="dropdown-item" href="#">Billing</a>
                    <a class="dropdown-item" href="#">Customize</a>*@
                                </div>
                            </li>
                        }
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link d-flex" asp-action="Logout" asp-controller="Account">
                                <i class="far fa-user mr-2 tm-logout-icon"></i>
                                <span>Logout @userManager.GetUserAsync(User).Result.UserName</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
</div>
